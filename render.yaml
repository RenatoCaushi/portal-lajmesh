services:
  - type: web
    name: konfidenciale
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: "python manage.py migrate && gunicorn portal_lajmesh.wsgi:application"